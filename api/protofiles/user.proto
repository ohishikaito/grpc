syntax = "proto3";

// Railsのmodelのclass名と干渉するので、package名はprotoにする
package proto;

option go_package = "./user";

service UserService {
  rpc GetUser(Empty) returns (User) {}
}

message Empty {}

message User {
  string name = 1;
}
