version: '3'
services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - ./:/app:cached
    tty: true
    ports:
      - "5300:5300"
    environment:
      - environment=development
      # - environment=production
      - GRPC_SERVICE_PORT=5300
    env_file:
      - .env

networks:
  default:
    external:
      name: go_default